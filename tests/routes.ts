/** Every page the site serves, used by most of the suites. */
export const ROUTES = [
  "/",
  "/work",
  "/about",
  "/writing",
  "/shows",
  "/writing/welcome",
  "/writing/building-this-site",
] as const;
