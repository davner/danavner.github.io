name: External links

# External links rot for reasons that have nothing to do with a commit - a
# venue folds, a profile moves, a host starts refusing bots. Checking them on
# every push would mean a red build nobody can fix, so they get their own
# weekly run instead, and internal links stay in CI where they belong.
on:
  schedule:
    - cron: "0 9 * * 1"
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  check:
    name: Check outbound links
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci
      - run: npm run build

      - name: Check links in the built site
        id: lychee
        uses: lycheeverse/lychee-action@v2
        with:
          # Social profiles routinely 403 an unauthenticated crawler; the point
          # is finding dead URLs, not fighting bot walls.
          args: >-
            --no-progress
            --max-retries 2
            --accept 200,206,429
            --exclude-path dist/assets
            --exclude 'linkedin\.com'
            --exclude 'instagram\.com'
            'dist/**/*.html'
            'src/content/**/*.md'
            'README.md'
          fail: false
          output: lychee-report.md

      - name: Open an issue if anything is dead
        if: steps.lychee.outputs.exit_code != 0
        uses: peter-evans/create-issue-from-file@v5
        with:
          title: Dead links found in the weekly check
          content-filepath: lychee-report.md
          labels: chore
